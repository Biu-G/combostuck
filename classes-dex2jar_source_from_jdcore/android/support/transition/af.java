package android.support.transition;

import android.os.IBinder;

class af
  implements ah
{
  private final IBinder a;
  
  af(IBinder paramIBinder)
  {
    a = paramIBinder;
  }
  
  public boolean equals(Object paramObject)
  {
    return ((paramObject instanceof af)) && (a.equals(a));
  }
  
  public int hashCode()
  {
    return a.hashCode();
  }
}
